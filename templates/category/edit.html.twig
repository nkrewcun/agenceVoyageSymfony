{% extends 'user_connected_base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block body %}
    <div class="container">
        <h2>Modifier la catégorie {{ category.nom }}</h2>
        {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.nom) }}
            {{ form_widget(form.nom) }}
            {{ form_errors(form.nom) }}
        </div>
        {{ form_widget(form.submit) }}
        {{ form_end(form) }}
    </div>
{% endblock %}

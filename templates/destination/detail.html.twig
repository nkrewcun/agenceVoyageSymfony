{% extends 'user_connected_base.html.twig' %}

{% block title %}Hello DestinationController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a
                            href="{{ 'C:/Users/Nathan/Desktop/Workspace/Symfony/agenceVoyage/src/Controller/DestinationController.php'|file_link(0) }}">src/Controller/DestinationController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ 'C:/Users/Nathan/Desktop/Workspace/Symfony/agenceVoyage/templates/destination/detail.html.twig'|file_link(0) }}">templates/destination/detail.html.twig</a></code>
            </li>
        </ul>

        <h2>
            {% if name == 'montagne' %}
                IL CAILLE
            {% elseif name == 'plage' %}
                Venez prendre un bain de soleil
            {% elseif name == 'campagne' %}
                Un peu de tranquillité dans la campagne auvergnate
            {% else %}
                Tu veux aller où alors ?
            {% endif %}
        </h2>

    </div>
{% endblock %}
